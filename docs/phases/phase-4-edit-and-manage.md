# Phase 4 — 저장 후 편집·관리 + 경량 카테고리 추천

> **이 문서 사용법**: Claude Code에 이 파일 전체를 컨텍스트로 넘기고 "Step N을 진행해달라"고 요청한다. Step은 순서대로 진행하고, 각 Step의 **DoD**를 사람이 확인한 뒤 다음으로 넘어간다. 아이폰 설치·실기기 동작은 에이전트가 검증할 수 없으므로 사람이 직접 한다.
>
> 관련 문서(원본): 노션 「WishShot v2 PRD (iOS 앱)」 §6.4(탐색/관리 FR-14~17)·§6.2(FR-8)·§10(설정 화면)·§9(예외), 「WishShot v2 — 플랫폼·아키텍처 결정 문서」. 이 문서는 그 둘에서 Phase 4에 필요한 부분만 **증류**한 작업 지시서다.
>
> ⚠️ **PRD와의 의도적 차이 2가지** (문서 확정 후 사람 확인을 거쳐 에이전트가 PRD를 이 문서에 맞춰 수정 예정 — Step 5):
> 1. **FR-8**을 PRD는 "다음(v2 릴리스 이후)"에 뒀으나, 이 Phase에서 **경량판(기존 카테고리 중 추천, 추가 LLM 호출·비용 없음)** 으로 앞당긴다. 범용 택소노미 분류(셔츠→"옷")는 여전히 이후.
> 2. **기존 데이터 이관**을 PRD는 Phase 4에 넣었으나, 옮길 실데이터가 테스트뿐이라 **범위에서 완전히 제외**한다(스크립트는 `docs/archive/`에 보존만).

---

## 목표 (Goal)

Phase 3까지 **저장(등록)** 이 완성됐다. Phase 4는 **저장한 뒤의 관리 경험**을 채운다. 이 Phase가 끝나면: 저장한 위시의 모든 필드를 편집하고, 다른 카테고리로 옮기고, 메모·태그를 달 수 있다. 또 등록 시 **기존에 만든 카테고리 중 가장 맞는 것을 자동으로 미리 선택**해 주고, **설정 화면**에서 로그아웃·개인정보 안내를 볼 수 있다.

**이 Phase의 핵심 검증(PRD S4 탐색/관리)**: 상세 → 편집 → 필드 수정·저장 → 목록/상세 반영. 카테고리 이동(미분류 포함). 메모·태그 입력·표시. 등록 시 카테고리 자동 미리선택. 설정에서 로그아웃.

## 이 Phase에서 하지 않는 것

- **기존 v1 데이터 이관** → **안 함.** `docs/archive/migrate-sqlite-to-supabase.ts`는 보존만(실행·개조 안 함).
- **TestFlight 내부 테스트 배포** → 디자인 개편 이후로 미룸(개편 Phase에 묶음).
- **E-2 원문 clipboard 복사, 크로스-소스 중복/제품명 후보 선택 UX** → PRD 근거 없는 구현 잔가지. 개편 때 다룸(크로스-소스 중복은 PRD 비목표).
- **FR-8 범용 카테고리 분류**(내 데이터와 무관한 "셔츠→옷/여성패션") → 이후. 이 Phase는 **기존 카테고리 매칭 경량판만**.
- 이미지 자르기, 검색/정렬 옵션, URL 공유 수신, Apple 로그인, Android → 이후.

---

## 사전 조건 (사람이 준비)

- [x] Phase 3 완료: OCR 자동채움, `parse-screenshot-text` Edge Function 배포, 등록/목록/상세/삭제 화면.
- [x] Supabase 대시보드 접근(또는 CLI 로그인) — Edge Function 재배포용.
- [x] `ANTHROPIC_API_KEY`가 Edge Function 시크릿에 등록돼 있음(Phase 3에서 완료).
- **네이티브 추가·EAS 재빌드 없음** — 이 Phase는 전부 **JS + Edge Function(서버 측)** 이라 앱 재빌드가 필요 없다. (`expo-image-picker`는 Phase 2에서 이미 추가됨.)

---

## 제약 (Constraints) — 에이전트가 지킬 것

Phase 1~3 규칙(한국어 문자열, 디자인 토큰, 비밀값 커밋 금지, 커밋 컨벤션, 문서 동기화, 데이터 레이어 경유, 모르면 묻기, 버전 임의 인상 금지)을 그대로 승계하고 다음을 추가한다.

1. **네이티브 의존성을 추가하지 않는다.** 이 Phase의 모든 기능은 JS 전용이거나 서버 측(Edge Function)이다. 네이티브가 필요해 보이면 멈추고 사람에게 묻는다(개편 Phase로 미룰 후보).
2. **편집 시 `normalized_name`을 반드시 재계산한다.** 브랜드·제품명을 바꾸면 중복 판정 키가 달라진다. 재계산은 **`src/lib/normalize.ts` 한 곳**만 쓴다(등록·중복조회와 동일 소스).
3. **편집이 중복을 만들면 차단한다.** 편집 결과 `normalized_name`이 **다른 아이템**과 겹치면 `UNIQUE(user_id, normalized_name)` 23505가 난다. 이를 잡아 "이미 같은 브랜드+제품명의 위시가 있어요"로 안내하고 저장을 막는다(자기 자신과의 일치는 정상). 등록 화면의 `DuplicateItemError`(Phase 2) 처리를 재사용하되, 편집 맥락에선 **덮어쓰기 없이 안내만** 한다.
4. **FR-8 추천은 기존 parse 호출에 얹는다.** 별도 Edge Function·별도 LLM 왕복을 만들지 않는다. `parse-screenshot-text`의 입출력만 확장한다(아래 계약). 추가 비용은 토큰 몇 개 수준.
5. **추천 카테고리는 반드시 사용자의 기존 목록 안에서만.** LLM이 목록 밖 값을 반환하면 앱이 무시한다(새 카테고리를 임의로 만들지 않는다). 추천이 없으면 지금처럼 사용자가 직접 고른다(폴백).
6. **카테고리 이름이 서버로 전송되는 것을 고지 대상에 포함한다(NFR-3 정합).** FR-8을 위해 사용자의 카테고리 이름 목록이 Edge Function/LLM으로 간다. 이는 사용자가 만든 라벨로 민감도가 낮고 이미 OCR 텍스트를 보내는 것과 같은 수준이지만, 개인정보 안내 문구가 "텍스트만 전송"과 어긋나지 않게 점검한다.
7. **데이터는 데이터 레이어(`src/lib/queries`)로만 접근한다.** 편집·이동·로그아웃 모두 쿼리 함수를 통한다. 화면에서 `supabase.from(...)` 직접 호출 금지.
8. **삭제·이동은 즉시 반영, `updated_at` 자동 갱신**(Phase 2 트리거 유지).

---

## FR-8 Edge Function 계약 변경 (Step 3 기준선)

`parse-screenshot-text`의 입출력을 **하위호환**으로 확장한다.

- **입력**: `{ "text": string, "categories"?: string[] }`
  - `categories`: 사용자가 이미 만든 카테고리 이름 목록. 없거나 빈 배열이면 추천을 건너뛴다(기존 동작 그대로).
- **출력**: `{ "productName": ..., "price": ..., "brand": ..., "confidence": ..., "suggestedCategory": string | null }`
  - `suggestedCategory`: **입력 `categories`에 포함된 값 중 하나**거나 `null`. 목록에 없는 값·새 이름을 지어내지 않는다(프롬프트로 강제 + 앱에서 목록 대조 검증).
  - `categories`가 비었으면 항상 `null`.
- **호환성**: `categories` 미전송 시 출력은 Phase 3와 동일(+`suggestedCategory: null`). 기존 호출부가 깨지지 않는다.
- **배포**: `npx supabase functions deploy parse-screenshot-text`(사람 실행). **앱 재빌드 무관**(서버 측).

---

## Step 1 — 아이템 편집 (FR-14) + 메모/태그 입력  ⭐

이 Phase의 핵심. 상세 화면에 "편집"을 붙이고, 저장한 모든 필드를 고칠 수 있게 한다.

**작업**
1. **데이터 레이어**(`src/lib/queries/items.ts`): 편집용 `updateItem`을 점검·보강한다. 입력으로 `product_name`·`brand`·`price`·`source_link`·`memo`·`tags`·`category_id`·`normalized_name`을 받아 해당 아이템을 UPDATE. `normalized_name`은 **호출부가 `normalizeName(brand, product_name)`으로 계산해 전달**한다. 23505(unique_violation)는 `DuplicateItemError`로 변환해 던진다(제약 3). (Phase 2 덮어쓰기에서 쓰던 update 경로가 있으면 재사용.)
2. **편집 화면 신설**: 상세에서 진입하는 편집 화면(라우트는 Expo Router 충돌 없는 경로로, 예: `item/[id]/edit` 또는 `item-edit/[id]` — 실제 경로는 `expo config`/타입 라우트로 검증). 기존 이미지(signed URL)를 보여주고, `제품명(필수)/브랜드/가격/링크/메모/태그/카테고리` 필드를 아이템 값으로 프리필. **OCR·분석 파이프라인은 붙이지 않는다**(이미 저장된 텍스트 편집이므로). 등록 화면(`register.tsx`)의 필드·카테고리 선택 UI를 **공통 컴포넌트로 추출해 재사용**하되, 분석 관련 로직은 제외.
3. **메모/태그**: 메모는 텍스트 입력(등록에도 이미 있음). **태그 입력 UX 신설** — 자유 입력 칩(입력 후 추가 → 칩, 칩 탭 → 제거). PRD §13 미결이므로 **자유 입력을 기본**으로 하고, 기존 태그 자동완성/선택은 선택적 개선(여력 되면). `tags text[]`에 저장.
4. **저장**: `normalizeName` 재계산 → `updateItem`. 성공 시 상세로 복귀·반영. 중복(다른 아이템과 정규화명 충돌) 시 제약 3대로 안내·차단.
5. **이미지 교체(선택)**: 여력이 되면 편집에서 이미지 교체(같은 Storage 키에 upsert, Phase 2 덮어쓰기 로직 재사용). 복잡하면 이 Phase에선 텍스트 필드만 편집하고 이미지 교체는 결과 기록에 "미포함"으로 남긴다.
6. 커밋: `feat: 아이템 편집 화면 (FR-14) + 메모/태그 입력`

**DoD**
- [x] (사람) 상세 → 편집 → 제품명/브랜드/가격/링크/메모 수정·저장 → 상세·목록에 반영
- [x] (사람) 태그 입력·삭제 → 상세에 칩으로 표시
- [x] 브랜드/제품명 변경 시 `normalized_name` 재계산(단일 소스). 다른 아이템과 겹치게 편집하면 저장 차단·안내
- [x] 편집 화면은 OCR/분석을 실행하지 않음. 디자인 토큰만·문자열 한국어. `tsc --noEmit` 통과
- [x] (추가) 이미지 교체 포함 — 같은 Storage 키 upsert, 필드 저장 통과 후 업로드

---

## Step 2 — 카테고리 이동 (FR-15)

**작업**
1. 편집 화면(Step 1)의 **카테고리 선택 필드**로 이미 이동이 가능하다. 여기에 더해 **상세 화면에서 편집을 거치지 않고 바로 카테고리만 바꾸는 빠른 이동** 액션을 제공한다(PRD §10 상세 = "카테고리 이동"). 카테고리 선택 모달/시트: 기존 카테고리 목록 + **미분류** + **새 카테고리 생성**(등록 화면의 카테고리 컴포넌트 재사용).
2. 데이터 레이어: `updateItem`으로 `category_id`만 바꾸거나, 명시적 `moveItemCategory(itemId, categoryId | null)` 헬퍼 추가(둘 중 하나로 단일화). `null` = 미분류.
3. 이동 후 목록·홈 카테고리 개수/썸네일이 갱신되게 한다(포커스 시 재조회).
4. 커밋: `feat: 아이템 카테고리 이동 (FR-15)`

**DoD**
- [x] (사람) 상세에서 카테고리 이동 → 원래 카테고리 목록에서 사라지고 새 카테고리에 나타남
- [x] (사람) 미분류로/에서 이동 정상. 홈의 카테고리 개수·대표 썸네일 갱신
- [x] `tsc --noEmit` 통과
- [x] (추가) 빈 카테고리 삭제/유지 안내(이동·편집·삭제 공통), 비운 뒤 '뒤로'는 홈으로, 빈 카테고리 홈 노출, 중복 이름 친화 에러

---

## Step 3 — 경량 카테고리 자동 추천 (FR-8)

기존 parse 호출에 얹어 **추가 비용·재빌드 없이** 등록 시 카테고리를 미리 선택해 준다.

**작업**
1. **Edge Function 확장**(위 계약): `parse-screenshot-text`가 `categories`를 받아 `suggestedCategory`(목록 내 값 또는 null)를 반환. 프롬프트에 "반드시 주어진 목록 중에서만 고르고, 맞는 게 없으면 null"을 명시. 하위호환 유지. 재배포(사람).
2. **`parse` 쿼리**(`src/lib/queries/parse.ts`): 호출 시 `listCategories()`의 이름 목록을 함께 전송, 응답의 `suggestedCategory`를 반환값에 포함. **목록에 없는 값은 무시**(제약 5).
3. **`useAnalysis`**: 정제 결과에 `suggestedCategory`를 실어 화면에 전달.
4. **등록 화면(`register.tsx`)**: `filled` 시 `suggestedCategory`가 있으면 **해당 카테고리를 미리 선택**하고 작은 "추천" 힌트를 표시. 사용자가 다른 카테고리를 고르면 그대로 덮인다(강제 아님). 추천이 없으면 지금처럼 미선택.
5. `analysis_logs.parsed`에 `suggestedCategory`도 남는지 확인(추천 정확도 평가용, 스키마 변경 없이 jsonb에 포함).
6. 커밋: `feat: 경량 카테고리 자동 추천 (FR-8, 기존 카테고리 매칭)`

**DoD**
- [x] 함수 재배포됨. `categories` 미전송 시 기존과 동일 응답(+`suggestedCategory: null`) — curl 실측 확인
- [x] (사람) 기존 카테고리와 맞는 스크린샷 등록 시 해당 카테고리가 미리 선택됨. 안 맞으면 미선택
- [x] 목록 밖 추천값은 앱이 무시(임의 카테고리 생성 안 함) — 서버·앱 화이트리스트
- [x] `tsc --noEmit` 통과

---

## Step 4 — 설정 화면 (로그아웃 + 개인정보 안내 열람)

PRD §10에 정의됐지만 아직 없는 설정 화면을 만든다.

**작업**
1. **설정 화면 신설**(`src/app/settings.tsx`): 로그인 이메일 표시, **로그아웃**, **개인정보 안내(NFR-3) 열람**(첫 업로드 시 1회 뜨는 고지 문구를 언제든 다시 볼 수 있게).
2. **로그아웃**: `auth` 쿼리에 `signOut()` 추가(`supabase.auth.signOut()`). 로그아웃 후 세션 라우팅 가드(`_layout.tsx`)가 로그인 화면으로 보냄.
3. **진입점**: 홈 화면에 설정 진입(예: 헤더 톱니 아이콘). 디자인 토큰 사용.
4. 개인정보 안내 문구는 Phase 3의 최초 고지와 **같은 문구를 단일 소스로** 재사용(중복 하드코딩 금지).
5. 커밋: `feat: 설정 화면 (로그아웃 + 개인정보 안내 열람)`

**DoD**
- [x] (사람) 홈 → 설정 → 로그인 이메일 확인 → 로그아웃 → 로그인 화면으로
- [x] (사람) 설정에서 개인정보 안내 다시 열람 가능
- [x] 문자열 한국어·디자인 토큰만. `tsc --noEmit` 통과
- [x] (추가) 로그아웃 후 콜드스타트 permission denied 해결(AuthGate 리다이렉트 중 보호 화면 마운트 차단)

---

## Step 4b — 태그 강화 (기존 태그 선택 + 태그별 모아보기)  ⭐ Phase 4 연장

원래 지시서 밖이지만 실기기 확인 중 사람 요청으로 추가(네이티브·재빌드 없음, 스키마 변경 없음 — `tags text[]` 재사용). PRD에 **FR-15a** 신설로 반영.

**작업**
1. **데이터 레이어**(`items.ts`): `listAllTags()`(내 모든 태그를 빈도순, RLS로 본인 것만) + `listItemsByTag(tag)`(`.contains('tags',[tag])`, 최신순).
2. **기존 태그 선택**: `TagInput`에 `suggestions` prop 추가 — 입력 밑 "이전에 쓴 태그" 칩(이미 단 건 제외), 탭 → 추가. 등록·편집 양쪽에서 `listAllTags()` 로드해 전달.
3. **태그별 모아보기**: 새 라우트 `src/app/tag/[name].tsx`(2열 그리드, `ItemCard` 재사용, 카테고리 무관). 상세의 태그 칩을 탭 가능하게 → `/tag/[name]`.
4. 커밋은 Step 5에 함께(문서 마무리와 한 커밋).

**DoD**
- [x] (사람) 등록·편집에서 "이전에 쓴 태그" 칩 노출·탭 추가, 이미 단 태그는 안 뜸. 자유 입력도 유지
- [x] (사람) 상세 태그 칩 탭 → 그 태그 아이템만 모아보기(다른 카테고리 것도 함께)
- [x] `tsc --noEmit`·`expo lint` 통과. 문자열 한국어·디자인 토큰만
- [ ] (이후) 태그 이름변경/삭제 일괄 관리 — 실사용 불편 시 추가

---

## Step 5 — 문서 정리 + PRD 갱신 + PR

**작업**
1. `CLAUDE.md` 갱신: 상태를 **Phase 4 완료**로. 편집/카테고리 이동/메모·태그, 설정 화면, FR-8 경량 추천(parse 계약 확장), 새 라우트·쿼리 반영. "이 Phase는 네이티브 추가·재빌드 없음" 명시.
2. `README.md`: 편집·설정 흐름, Edge Function 재배포(계약 확장) 안내 갱신.
3. 이 문서 DoD 채우고 "결과 기록" 작성(편집 라우트, 태그 입력 방식, 중복 편집 처리, FR-8 프롬프트/정확도 관찰, 이미지 교체 포함 여부).
4. **PRD 갱신**: 노션 「WishShot v2 PRD (iOS 앱)」를 이 문서에 맞춰 갱신한다. 절차: **(1) 에이전트가 변경할 부분을 텍스트로 먼저 정리해 사람에게 보여준다 → (2) 사람이 확인·승인 → (3) 승인된 경우에만 에이전트가 노션 MCP로 직접 수정한다.** 변경 요지:
   - FR-8: "다음(v2 릴리스 이후)" → **Phase 4 경량판**(기존 카테고리 매칭)으로.
   - §7 릴리스 스코프: **기존 데이터 이관을 Phase 4에서 제거**.
   - v2 코어 각주의 **TestFlight 배포를 "디자인 개편 이후"로** 이동.
   - (사람이 승인 전 수정을 원하면 그 지시대로 반영 후 다시 확인받는다.)
5. 커밋: `docs: Phase 4 완료 — 문서 갱신, 결과 기록`. `dev`로 PR.

**DoD**
- [x] `CLAUDE.md`가 현재 구조를 정확히 설명(없는 걸 있다고 쓰지 않음) — 편집/이동/태그·모아보기/설정/FR-8/빈 카테고리 반영
- [x] (사람) PRD 변경 요지 확인·승인 → 에이전트가 노션 반영(3건 + 빈 카테고리 + FR-15a)
- [x] `dev`로 PR (열림 — 사람이 머지)

---

## 미결 사항 (진행 중 결정, 결과 기록에 남길 것)

- **편집 화면 형태**: 별도 화면 vs 상세 인라인 편집. 라우트 경로(Expo Router `[id].tsx`와 `[id]/` 공존 여부) 확정.
- **태그 입력 UX**(PRD §13 미결): 자유 입력 기본. 기존 태그 자동완성/선택 추가 여부.
- **이미지 교체 포함 여부**: 텍스트 필드만 vs 이미지까지. 복잡도 보고 결정.
- **중복 편집 정책**: 다른 아이템과 정규화명 충돌 시 "차단·안내"로 확정(덮어쓰기 제공 안 함) — 실사용 후 추가 UX 필요하면 보강.
- **FR-8 추천 힌트 UI**: 미리선택만 vs "추천" 배지까지. `confidence`와 연동할지.
- **설정 진입점 위치**: 홈 헤더 아이콘 vs 별도 탭.

**이 문서에서 확정한 결정:**
- 범위 = 편집(FR-14)·카테고리 이동(FR-15)·메모/태그 + FR-8 경량 추천 + 설정 화면. **재빌드 없음.**
- **v1 데이터 이관: 범위에서 제외**(archive 보존만).
- **FR-8은 경량판만**(기존 카테고리 매칭, parse 호출에 얹음). 범용 분류·별도 호출 없음.
- 편집이 중복을 만들면 **차단·안내**(덮어쓰기 없음).
- 이미지 교체는 **선택**(코어는 텍스트 필드 편집), 태그 입력은 **자유 입력 칩** 기본.
- TestFlight·clipboard·크로스소스 중복은 **개편 이후**.
- PRD 갱신은 **사람 확인 후 에이전트가 노션 직접 수정**(Step 5).

## 결과 기록 (Phase 완료 시 작성)

- **완료일**: 2026-09-14
- **편집 화면 / 라우트 / 재사용 컴포넌트**: 상세 `item/[id].tsx`를 `item/[id]/index.tsx`로 디렉터리화하고 편집 화면 `item/[id]/edit.tsx` 신설(상세 헤더에 "편집" 진입점). 등록 화면의 인라인 폼을 공용 컴포넌트 `FormField`(+`formInput` 스타일)·`CategoryPicker`·`TagInput`(`src/components/`)로 추출해 등록·편집이 공유. 편집은 `getItem` 프리필만, **OCR·분석 파이프라인 없음**. `experiments.typedRoutes: true`라 새 라우트 추가 시 `expo start`로 타입 라우트 재생성이 필요(그 전엔 `tsc`가 새 경로에서 실패 — 코드 문제 아님).
- **태그 입력 방식**: **자유 입력 칩 + 기존 태그 선택**(`TagInput`) — 입력 후 완료(Enter)로 칩 추가, 칩 탭으로 제거, 중복·공백 무시. 입력 밑에 **"이전에 쓴 태그"**(내 모든 아이템의 태그를 빈도순으로, `listAllTags`)를 선택 칩으로 보여줘 탭하면 추가(이미 단 건 제외). `tags text[]`에 저장. 등록·편집 **양쪽**에 도입. **태그별 모아보기(FR-15a)**: 상세의 태그 칩을 탭하면 `tag/[name]` 라우트에서 그 태그가 달린 아이템만 **카테고리 무관**으로 모아 본다(`listItemsByTag` = Supabase `.contains('tags',[tag])`). 태그 이름변경/삭제 일괄 관리는 이후(실사용 보고 판단).
- **중복 편집 처리**: `updateItem`이 `normalizeName` 재계산 → **다른 아이템**과 겹치면 23505를 `DuplicateItemError`로 변환해 **차단·안내**(덮어쓰기 없음, 자기 자신은 값 불변이면 위반 없음). `moveItemCategory`는 `category_id`만 바꿔 정규화명 불변 → 중복 위험 없음.
- **FR-8 계약 확장 / 프롬프트 / 관찰된 추천 정확도**: `parse-screenshot-text` 하위호환 확장 — 입력 `categories?: string[]`, 출력 `suggestedCategory: string | null`. 프롬프트로 "주어진 목록에서 **정확히 하나 복사**, 없으면 null, 새 이름 금지" 강제 + **서버·앱 양쪽 화이트리스트**(목록 밖 값 무시). `categories` 미전송/빈 배열이면 항상 `null`(실측 확인: `{text}`만 보내도 `suggestedCategory:null`). 실기기에서 기존 카테고리와 맞는 스크린샷 → 해당 카테고리 미리선택 + "AI가 추천한 카테고리예요" 힌트 확인. 정확도는 실사용 관찰 대상(경량판 — 범용 택소노미 분류 아님).
- **이미지 교체 포함 여부**: **포함.** 편집에서 사진 교체(탭/"다른 사진 선택") → 같은 Storage 키에 `upsert`(image_key 불변, DB 필드 변경 없음). **필드 저장(`updateItem`)이 통과한 뒤에만** 이미지 업로드 → 중복 차단 시 이미지 안 바뀜. 상세 복귀 시 signed URL 재발급으로 새 이미지 반영.
- **설정 화면 / 로그아웃**: `settings.tsx`(`/settings`) — 로그인 이메일·개인정보 안내(NFR-3) 상시 열람·로그아웃. `auth`에 `signOut()`·`getCurrentUserEmail()` 추가. 로그아웃은 `_layout` AuthGate의 `onAuthStateChange`로 로그인 화면 이동. 개인정보 문구는 `src/constants/privacy.ts` 단일 소스(register 최초 고지와 공유). 홈 헤더 "로그아웃"→"설정" 진입점으로 교체.
- **발생한 이슈와 해결**:
  1. **모달 위 `Alert.prompt`(iOS)** 로 새 카테고리 생성·이동이 조용히 실패 → 시트 안 **인라인 입력**("만들고 이동")으로 전환.
  2. **빈 카테고리 UX 반복**: 홈에서 빈 카테고리도 **노출**하도록 변경(Phase 2 "빈 카테고리 비노출" 규칙을 뒤집음 — 삭제 여부 인지 위해). 이동·**편집·아이템 삭제** 어느 동작으로든 카테고리가 0이 되면 `promptDeleteIfCategoryEmpty`로 삭제/유지 안내. 안내 **응답을 await**한 뒤 이동해야 홈이 삭제 결과를 즉시 반영(초기엔 얼럿 전에 이동해 미반영됐음).
  3. **비운 뒤 '뒤로' 내비게이션**: 원래 카테고리를 비운 뒤 상세 '뒤로'는 (잔상 있는) 카테고리 화면을 건너뛰고 **홈으로**(`dismissAll`). 삭제된 카테고리 화면 진입 시 홈으로 리다이렉트(안전망).
  4. **중복 카테고리명 에러**: 원본 `permission ... unique constraint` 메시지 대신 `DuplicateCategoryError`("동일한 이름의 카테고리가 있어요") — 원본은 `console.warn`으로만.
  5. **저장 후 이동**: 새 아이템 저장 시 홈이 아니라 **담은 카테고리 목록**으로 이동(맥락 확인).
  6. **로그아웃 콜드스타트 `permission denied`**: 로그아웃 상태로 앱을 새로 켜면 로그인 리다이렉트 직전 홈이 잠깐 마운트돼 anon으로 `categories`/`items`를 조회 → AuthGate가 **리다이렉트 중엔 보호 화면 대신 스피너**를 렌더해 조회 자체를 차단.
  7. **typedRoutes 생성 파일**(`.expo/types/router.d.ts`)이 재생성되며 새 라우트를 잃어 `tsc`가 반복 실패 → `expo start`로 재생성하면 통과(코드 문제 아님, `.expo`는 gitignore).
- **PRD 갱신 반영(사람 승인 후 에이전트가 노션 직접 수정)**: FR-8 이후→Phase 4 경량판(§6.2) · 데이터 이관 Phase 4에서 제거(§7) · TestFlight 개편 이후로(§7) · **빈 카테고리 노출로 변경(FR-11·§11)** · **태그 UX 확정 + FR-15a 신설(§6.4·§13)**.
- **다음(개편/이후)으로 넘길 것**: TestFlight 배포, E-2 clipboard, 크로스소스 중복/제품명 후보 UX, FR-8 범용 분류, 검색/정렬·이미지 자르기·URL 공유·Apple 로그인·Android.
